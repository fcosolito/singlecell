- write the necessary classes for loading (just to memory) the files
That is, most of the model classes, a controller for testing this and 
some others for the file access
- Learn to use threads
- Can use OpenCSV to read files
- Look for a better way (faster) or native way for input data
- Set some root paths for the files in the app props
- Learn about logs, they could be useful for debugging right now